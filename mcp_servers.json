{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "TrueForge Local & Remote MCP Tool Server Registry",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "Repository file access, PR creation, and issue management"
    },
    "slack": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}"
      },
      "description": "Send alerts, triage discussions, and interactive incident notifications"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./workspace"],
      "description": "Safe local file manipulation within project workspace"
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "${DATABASE_URL}"],
      "description": "Read-only schema inspection and SQL diagnostic queries"
    },
    "exa-search": {
      "url": "https://mcp.exa.ai/sse",
      "transport": "sse",
      "auth": {
        "type": "none"
      },
      "description": "High-accuracy live web search and deep scraping"
    }
  }
}
